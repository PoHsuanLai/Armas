# Text Reveal Card

Card with text reveal effect on hover.

```demo
let mut text_reveal = TextRevealCard::new(300.0, 200.0, "Hover to reveal".to_string(), "Hidden message!".to_string());
text_reveal.show(ui);
```
